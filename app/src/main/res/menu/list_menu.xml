<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <item
        android:id="@+id/app_bar_search"
        android:icon="@drawable/ic_search"
        android:title="@string/search_item_menu"
        app:actionViewClass="android.widget.SearchView"
        app:showAsAction="ifRoom|collapseActionView" />
    <item
        android:id="@+id/add_word"
        android:icon="@drawable/ic_add"
        android:title="@string/add_item_menu"
        app:showAsAction="ifRoom|withText" />
    <group android:id="@+id/change_words">
        <item
            android:id="@+id/switch_en_to_pr"
            android:checked="true"
            android:title="@string/persian_switch"
            android:visible="false"
            app:actionLayout="@layout/switch_item"
            app:showAsAction="never" />
        <item
            android:id="@+id/switch_pr_to_en"
            android:checked="false"
            android:title="@string/english_switch"
            app:actionLayout="@layout/switch_item"
            app:showAsAction="never" />
    </group>
</menu>